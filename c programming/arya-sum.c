#include <stdio.h>
#include <math.h>
#include <limits.h>
#include <stdlib.h>
int main()
{
    int n, i, arr[10000];
    scanf("%d", &n);
    for (i = 0; i < n; i++)
        scanf("%d", &arr[i]);
    int max_sum = INT_MAX;
    int current_sum = 0;
    int product = 0;
    for (i = 0; arr)
}