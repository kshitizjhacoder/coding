#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, ele, i = 3;
        cin >> n;
        vector<int> r, b, g;
        while (i--)
        {
            cin >> ele;
            r.push_back(ele);
            cin >> ele;
            b.push_back(ele);
            cin >> ele;
            g.push_back(ele);
        }
        }
}