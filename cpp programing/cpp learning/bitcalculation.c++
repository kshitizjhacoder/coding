#include <bits\stdc++.h>
using namespace std;

int main()
{
    // std ::bitset<10> x; // assign the value bitwise;
    // x.set();            // set all the value to one;
    // cout << x << endl;
    std::bitset<4> x(4);
    x.flip();
}