#include <iostream>
using namespace std;

void duprem(string s,char sym)
{
    if(sym == )
}
int main()
{
    string s;
    cin >> s;
    for(int i=0;s[i] != '\0';i++)
    {
        duprem(s,s[i]);
    }
}